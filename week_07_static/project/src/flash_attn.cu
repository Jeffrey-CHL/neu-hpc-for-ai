// The real CUDA source is embedded in main.py and written at runtime to avoid
// local file mutation issues during Modal image builds.
#include <stdio.h>
int main(){ printf("Placeholder. Use main.py to generate real CUDA code.\n"); return 0; }
